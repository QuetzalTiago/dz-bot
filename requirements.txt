# Pinned runtime dependencies. Regenerate deliberately after testing an
# upgrade; unpinned builds were previously non-reproducible.
#
# Note: the ffmpeg/ffprobe binaries are installed via the OS package manager
# (see Dockerfile), NOT pip -- the old "ffmpeg"/"ffprobe" PyPI entries were
# unrelated placeholder packages and have been removed.
discord.py==2.7.1
yt-dlp==2026.7.4
PyNaCl==1.6.2
spotipy==2.26.0
pytz==2026.2
Pillow==12.3.0
SQLAlchemy==2.0.51
PyMySQL==1.2.0
beautifulsoup4==4.15.0
google-generativeai==0.8.6
grpcio==1.81.1
proto-plus==1.28.0
cryptography==49.0.0
aiohttp==3.14.1
